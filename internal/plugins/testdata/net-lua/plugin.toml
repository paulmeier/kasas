name         = "net-lua"
version      = "0.1.0"
description  = "Exercises kasas.fetch wiring (Lua)"
runtime      = "lua"
hooks        = ["OnTransactionCreate"]
capabilities = ["labels:write", "net:fetch"]

[net]
allow = ["api.example.com"]
